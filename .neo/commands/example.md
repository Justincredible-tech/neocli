# Example Custom Command

This is an example custom slash command. When you run `/example`, this prompt will be sent to Neo.

## Usage

Create your own commands by adding `.md` files to this directory. The filename becomes the command name (e.g., `deploy.md` becomes `/deploy`).

## Variables

You can use `{{args}}` to insert any arguments passed to the command.

---

**Your task:** Explain what custom slash commands are and how to create them in NeoCLI.

Arguments provided: {{args}}
